<%= render 'layouts/common/header', title: Car.model_name.human(count: 2), subtitle: 'List' %>
<div class="row">
  <div class="col-md-12">
    <%= render 'list' %>
    <% if policy(Car.new).create? %>
      <div class="list-action-buttons">
        <%= render 'layouts/common/buttons/new',
                  link: new_car_path %>
      </div>
    <% end %>
  </div>
</div>

